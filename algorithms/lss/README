cuda-basic:
	Basic implementation of the algorithm, without optimizations.
cuda-basic-4
	Basic implementation of the algorithm, processing 4 pixels per thread.
cuda-opt
	Optimized implementation of the algorithm, processing 4 pixels per thread and Z-order.
cuda-opt-dp-bf:
	Optimized implementation of the algorithm + running Dynamic Parallelism with 1 thread per block.
cuda-opt-nodp:
	Optimized implementation of the algorithm without dynamic parallelism (using omp and concurrent kernel execution).
